# v1.2 J6 September 2 restoration

Source: dfe5f4f (2026-09-02). Only the v1.2 wrist_yaw2 runtime estimator is restored.

- Capture accepted marker poses with paired measured encoder vectors.
- Historical robust encoder-constrained circle fitting; FK initializes camera-frame axis direction/sign.
- Transform fitted J6/J5 axes using each sweep midpoint marker rotation.
- Use saved version-specific bracket reference, generic reference, then nominal fallback.
- Historical candidate: 0.8 * raw angle + measured J6 angle. Subtract staged offset before handing the result to the current relative-update controller.
- Preserve visibility recovery, shortened sweep ranges, 20-second J6 sweep, current intrinsics/PnP/auto exposure, J5 behavior and 0.06-degree convergence threshold.
- No full-frame axis consistency gate on the restored path. Keep existing 5-degree observation arc / 0.5-mm fit RMS limits and reject degenerate data. Rejected observations can request realignment; failures never become zero corrections.
- Historical optimizer residual parameter slices are copied to avoid mutating optimizer inputs.

Offline tests cover known correction sign and magnitude, absolute-to-relative conversion, midpoint policy, invalid encoders, stationary observations, FK/reference adapter, paired encoder acquisition, and full-sweep recovery. No physical robot commands or home-offset writes were performed. Existing pose-only recordings cannot replay the full historical algorithm because encoder samples were not recorded.
