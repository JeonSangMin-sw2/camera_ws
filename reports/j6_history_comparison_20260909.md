# 9월 6일 이전과 현재 J6 구현 비교

Git 기록상 2026-09-06 00:00 KST 이전의 마지막 커밋은
`dfe5f4f` (2026-09-02 18:17:43, update)다.
이는 저장소의 버전이며 그 시점 실물에서 실행한 미커밋 코드까지 증명하지는 않는다.
소스 열람으로 비교했으며 로봇 실행, 알고리즘 교체, 허용값 변경은 하지 않았다.

## 직접적인 중단 조건 도입

`40ad8eb` (2026-09-08 20:23:41, debug)에서
`core/calibration/joint_reference.py`의 `_marker_axis`가 추가됐다.
전체 프레임에 대해 Rᵀn을 계산하고 0.5° 이내 비율 80% 이상 및
앞/뒤 구간 차이 0.5° 이하를 요구한다. 지금의 오류 문구
`Inconsistent marker-frame sweep axes`도 이 커밋에서 도입됐다.

`ce00cee` (2026-09-09 04:03:37, update_it need debug)에서 이 코드는
현재 `core/calibration/JointCalibrator.py`로 옮겨졌다.

9월 2일 `MarkerCalibrator.compute_unified_bracket_calibration` (510행부터)는
관측 원의 축이 있으면 스윕의 중간 프레임 하나의 R_ref로 `R_ref.T @ n_cam`을
계산했다. 전체 프레임 일관성으로 거부하는 현재 검사는 없었다.
관측 축이 없으면 중간 프레임 대비 상대 회전축을 평균하는 대체 경로를 썼다.

따라서 같은 축 흔들림이 있어도 이전 경로는 지금의 검사 때문에 중단되지는 않는다.
이것만으로 이전 결과가 틀렸거나 정확했다고 단정할 수는 없다.

## 측정 입력과 추정 방식도 변경됨

| 항목 | dfe5f4f (9월 2일) | 현재 |
|---|---|---|
| 내부 파라미터 | use_calib_int=False, RealSense 공장값 | camera_intrinsics.yaml 고정 사용 |
| PnP | SOLVEPNP_IPPE | SOLVEPNP_IPPE 유지 |
| 관절 원 피팅 | 엔코더 각도 및 FK 기반 axis_prior 전달, robust 피팅 | 순서 있는 관측 위치 기반 원 피팅 |
| 마커 좌표계로 축 변환 | 주로 중간 프레임 R 한 개 | 전체 프레임 R 및 일관성 검사 |
| J6 기준 브래킷 | 저장된 Tf_to_marker 값 우선 | robot_config의 nominal_brackets |
| J6 갱신 | raw_diff × 0.8 + 현재 J6 위치의 절대 후보 | 측정 상대 보정량을 staged 값에 누적 |

내부 파라미터 변경은 `f6c973f` (2026-09-08 16:30:51,
update_with_gpt)에서 소스 선택을 도입하고 setting.yaml을
`intrinsics_source: calibrated`로 설정한 때부터 확인된다.
`ce00cee`에서 공장값 선택 경로가 제거되고 파일 로딩으로 통합됐다.

이전 코드의 cornerSubPix 및 SOLVEPNP_IPPE 호출 자체는 현재와 동일하다.
따라서 최근에 IPPE 알고리즘을 새로 도입해서 발생한 문제라는 설명은 맞지 않는다.
같은 PnP라도 내부 파라미터가 바뀌면 계산한 자세가 달라질 수 있으며,
공장값/파일값 중 어느 쪽이 실제로 더 정확한지는 별도의 동일 영상 비교가 필요하다.

9월 2일 J6 내부 계산에는 예외 시 보정값을 0으로 대체하는 경로도 있었다.
현재는 실패를 명시하고 보정값을 반환하지 않는다. 해당 대체 경로가 과거 실제
실행에서 발생했다는 증거는 없으며, 과거 성공을 이 경로 탓으로 단정하지 않는다.

## 결론

이번 오류의 직접적인 중단 지점은 9월 8일 추가된 전체 프레임 일관성 검사다.
동시에 내부 파라미터, 원 피팅 입력, 기준 브래킷과 갱신 규칙도 바뀌었다.
그러므로 실제 관측 품질의 변화와 새 검사에 의한 동작 변화를 구분해야 하며,
하드웨어 고장이나 PnP 단독 문제를 먼저 확정하는 것은 근거가 부족하다.

다음 비교는 기준을 유지한 상태에서 동일 원본 영상에 공장/파일 내부 파라미터를
각각 적용하고, 과거·현재 추정의 차이를 분리하는 방식이 적절하다.
현재 저장한 pose만으로는 과거 공장 파라미터 기반 pose를 정확히 복원할 수 없다.
